// Your code here...
#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int n, m, s;
        scanf("%d %d %d", &n, &m, &s);
        int prankFriend = findPrankReceiver(n, m, s);
        printf("%d\n", prankFriend);
    }

    return 0;
}
